export interface MarketplaceItem {
  id: number;
  image: string;
  title: string;
  description: string;
}

export interface CTASection {
  headline: string;
  description: string;
  buttonText: string;
  buttonLink: string;
}
